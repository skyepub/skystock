server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://home.skyepub.net:63306/skystock?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: "Mako2122!"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
